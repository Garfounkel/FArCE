// Caracteristique = (threshold, toggle, error, margin)


// algorithme 4

Caracteristique find_Decision_Stump(training_examples* n_training_examples, weight* probabilistic_example_weights)
{

  threshold t = min(n_training_examples);
  margin M = 0;
  error e = 2;

  //Sum up the weights of the positive (resp. negative) examples whose f-th feature is bigger than the present threshold

  weight Wp1 = sum();



}
